"use client";

import { useState } from "react";
import { Camera, Expand, Image as ImageIcon, Tag, UsersRound, X } from "lucide-react";

type Album = { id: string; title: string; description: string | null; photos: { id: string; imageUrl: string; caption: string | null; tags: string[] }[] };
export default function GalleryClient({ albums }: { albums: Album[] }) {
  const [selected, setSelected] = useState<Album["photos"][number] | null>(null);
  return <div className="space-y-7"><section className="page-heading"><div><p className="data-label">NISHETA / фотогалерея</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.06em] text-graphite sm:text-4xl">Не мемы. Наши моменты.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-graphite-muted">Фото появляются в публичной галерее только после проверки. В каждом кадре можно сохранить подпись и отметить игроков.</p></div></section>{albums.length ? <div className="space-y-7">{albums.map((album) => <section key={album.id} className="surface overflow-hidden"><div className="flex flex-wrap items-end justify-between gap-3 border-b border-hairline px-5 py-5 sm:px-6"><div><p className="data-label">Альбом</p><h2 className="mt-1 text-2xl font-semibold tracking-[-0.06em] text-graphite">{album.title}</h2>{album.description && <p className="mt-2 text-sm text-graphite-muted">{album.description}</p>}</div><p className="text-xs font-semibold text-graphite-muted">{album.photos.length} фото</p></div><div className="grid grid-cols-2 gap-px bg-hairline sm:grid-cols-3 lg:grid-cols-4">{album.photos.map((photo) => <button key={photo.id} onClick={() => setSelected(photo)} className="group relative aspect-square overflow-hidden bg-paper-muted text-left"><img src={photo.imageUrl} alt={photo.caption ?? `Фото из альбома ${album.title}`} className="h-full w-full object-cover transition duration-300 group-hover:scale-105"/><span className="absolute inset-0 flex items-center justify-center bg-graphite/25 opacity-0 transition group-hover:opacity-100"><Expand className="text-paper" size={20}/></span></button>)}</div></section>)}</div> : <section className="surface grid min-h-[390px] place-items-center p-8 text-center"><div><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-paper-muted text-[#8d7552]"><Camera size={24}/></span><p className="data-label mt-6">Галерея ожидает первый альбом</p><h2 className="mt-2 text-2xl font-semibold tracking-[-0.06em] text-graphite">Лучшие кадры появятся здесь.</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-graphite-muted">Администратор создаёт альбом, загружает фотографии в черновики, отмечает игроков и публикует только проверенный материал.</p><div className="mx-auto mt-7 grid max-w-xl gap-2 sm:grid-cols-3">{[[ImageIcon, "Альбомы"], [Tag, "Подписи"], [UsersRound, "Теги игроков"]].map(([Icon, label]) => { const ItemIcon = Icon as typeof ImageIcon; return <div key={String(label)} className="rounded-xl bg-paper-muted p-3 text-center"><ItemIcon className="mx-auto text-[#987d56]" size={16}/><p className="mt-2 text-[10px] font-semibold text-graphite">{String(label)}</p></div>; })}</div></div></section>}{selected && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-graphite/90 p-4" role="dialog" aria-modal="true"><button onClick={() => setSelected(null)} className="absolute right-5 top-5 rounded-xl bg-paper/10 p-2 text-paper" aria-label="Закрыть"><X size={20}/></button><div className="max-h-full max-w-5xl overflow-auto rounded-2xl bg-graphite"><img src={selected.imageUrl} alt={selected.caption ?? "Фотография NISHETA"} className="max-h-[75vh] w-full object-contain"/><div className="flex flex-wrap items-center justify-between gap-3 p-5 text-paper"><p className="text-sm text-paper/85">{selected.caption ?? "Без подписи"}</p><div className="flex flex-wrap gap-1.5">{selected.tags.map((tag) => <span key={tag} className="rounded-md bg-paper/10 px-2 py-1 text-[10px] font-semibold">{tag}</span>)}</div></div></div></div>}</div>;
}
